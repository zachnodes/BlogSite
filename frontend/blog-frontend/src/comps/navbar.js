import React from 'react';
import navbar from '../styles/Nav.module.css'

const Navbar = () => {
    return (
        <div >
            <nav className={navbar.navbar}>Logo</nav>
        </div>
    );
}

export default Navbar;
